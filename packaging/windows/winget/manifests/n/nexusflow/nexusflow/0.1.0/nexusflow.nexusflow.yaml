# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.6.0.schema.json
# NOT validated — no Windows environment in this dev sandbox to run
# `winget validate` / `winget install --manifest` against (Marco 11). Update
# PackageVersion + the two companion manifests together before submitting to
# microsoft/winget-pkgs; the real submission also needs a signed
# nexusflow.msi (see ../../main.wxs) with a real InstallerUrl + Sha256.
PackageIdentifier: NexusFlow.NexusFlow
PackageVersion: 0.1.0
ManifestType: version
ManifestVersion: 1.6.0
DefaultLocale: en-US
