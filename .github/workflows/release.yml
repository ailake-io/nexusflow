name: Release

# Builds nexusflow-<os>-<arch>.tar.gz for Linux/macOS x86_64+arm64 and
# publishes them as GitHub Release assets — what scripts/install.sh
# downloads. Each tarball is: the release binary (--features
# embed-ui,connectors-all — every connector linked in, not just the two
# always-on ones, so GET /connectors lists the full catalog out of the box)
# plus the two ADBC driver libraries nexus-connector-postgres/-sqlite dlopen
# at runtime (no crates.io distribution of those exists — see
# nexus-connector-postgres/src/driver.rs, ARCHITECTURE.md §3).
#
# All four builds are native (no cross-compilation): GitHub's own
# ubuntu-24.04-arm hosted runner covers linux/arm64 natively, same as
# macos-14 (Apple Silicon) / macos-13 (Intel) do for the two macOS arches —
# avoids cross-compiling the ADBC C++ sources for a foreign arch/libc, which
# scripts/build-adbc-*-driver.sh isn't set up for.

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: linux
            arch: x86_64
            runner: ubuntu-latest
          - os: linux
            arch: arm64
            runner: ubuntu-24.04-arm
          - os: macos
            arch: x86_64
            runner: macos-13
          - os: macos
            arch: arm64
            runner: macos-14
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@1e60f622b4bbe3a6edab36a8db22d6e1d1e4c0d1 # v4.0.3
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # unixodbc-dev/libsasl2-dev/libssl-dev/zlib1g-dev are here for
      # connectors-all's two natively-linked connectors — nexus-connector-odbc
      # (odbc-api needs a driver manager to build/link against, ARCHITECTURE.md
      # §3) and nexus-connector-kafka (rdkafka's default "cmake-build" feature
      # compiles vendored librdkafka from source, which auto-detects SASL/SSL/
      # zlib and silently builds without those protocols if headers are
      # missing — installed here so the shipped binary has them).
      - name: Install native build deps for the ADBC drivers + connectors-all
        if: matrix.os == 'linux'
        run: |
          sudo apt-get update && sudo apt-get install -y \
            cmake pkg-config libpq-dev libsqlite3-dev libfmt-dev \
            unixodbc-dev libsasl2-dev libssl-dev zlib1g-dev

      - name: Install native build deps for the ADBC drivers + connectors-all (macOS)
        if: matrix.os == 'macos'
        run: brew install cmake pkg-config libpq sqlite fmt unixodbc

      - run: npm ci
        working-directory: frontend
      - run: npm run build
        working-directory: frontend

      - run: ./scripts/build-adbc-postgresql-driver.sh target/adbc
      - run: ./scripts/build-adbc-sqlite-driver.sh target/adbc

      - run: cargo build --release -p nexusflow --features embed-ui,connectors-all

      - name: Package tarball
        run: |
          lib_ext=so
          [ "${{ matrix.os }}" = "macos" ] && lib_ext=dylib
          stage="$(mktemp -d)"
          cp target/release/nexusflow "$stage/nexusflow-bin"
          cp "target/adbc/libadbc_driver_postgresql.$lib_ext" "$stage/"
          cp "target/adbc/libadbc_driver_sqlite.$lib_ext" "$stage/"
          tar -czf "nexusflow-${{ matrix.os }}-${{ matrix.arch }}.tar.gz" -C "$stage" .

      - uses: actions/upload-artifact@v4
        with:
          name: nexusflow-${{ matrix.os }}-${{ matrix.arch }}
          path: nexusflow-${{ matrix.os }}-${{ matrix.arch }}.tar.gz

  docker-publish:
    name: docker-publish
    needs: build
    runs-on: ubuntu-latest
    # linux/arm64 compiles connectors-all's full Rust workspace under QEMU
    # emulation (no native arm64 Docker builder wired up here, unlike the
    # `build` job above which uses a real ubuntu-24.04-arm runner for the
    # tarball) — emulated cargo builds run several times slower than
    # native, hence the generous timeout versus the amd64-only job this
    # replaced (60min).
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: docker/setup-qemu-action@29109295f81e9208d7d86ff1c6c12d2833863392 # v3.6.0
        with:
          platforms: arm64
      - uses: docker/setup-buildx-action@c47758b77c4736b95dc5a9f63504ba8c8d8a0670 # v3.7.1
      - uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@369eb591f429131d6889c4571c5e980676ea0ef6 # v5.6.1
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}

      - uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a6 # v6.15.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish:
    needs: [build, docker-publish]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@65a9edc5881444af0b9093a83ba2535f647f564c # v4.1.7
        with:
          path: dist
          merge-multiple: true
      - name: Generate SHA256SUMS
        run: |
          cd dist
          sha256sum *.tar.gz > SHA256SUMS
      - uses: softprops/action-gh-release@c062e08bd532815e2082a85e87e3ef5c1253cc56 # v2.2.0
        with:
          files: |
            dist/*.tar.gz
            dist/SHA256SUMS
          generate_release_notes: true
